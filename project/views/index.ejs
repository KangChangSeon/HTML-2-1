      <!-- 상단 소개글, 히어로 이미지 -->
      <div class="top">
        <h1 class="top-heading">메인 홈페이지</h1>
        <p class="top-body">살려주세요</p>
      </div>
      
      <img src="/img/img1.jpg" alt="imgtest" class="hero-image" width="840" height="400">

      <!-- 최근 게시물 -->
      <section class="articles">
        <h2 class="articles-heading">최근 게시물</h2>
        <ul class="article-ul">
          <% data.forEach(post => { %>
            <li>
              <a href="/post/<%= post._id %>">
                <span><%= post.title %></span>
                <span class="article-list-date"><%= post.createdAt.toDateString() %></span>
              </a>
            </li>
            <% }) %>
        </ul>
      </section>
      <p style="text-align: right;">@made by Kang Chang Seon</p>

